# TpFinalCA

## Procedimiento de ejecución
Cada codigo tiene dentro suyo el script de compilación y el de ejecución

En el caso de omp, se debe intalar "sudo apt install -y build-essential pkg-config mpich libcsfml-dev", luego se ejecuta primero "export OMP_NUM_THREADS=4" para asignar la cantidad de hilos y luego si se ejecuta el script para señalar con que imagenes trabajar.

El resto de codigos simplemente se compila con el script que esta al inicio de cada codigo y luego se ejecuta
